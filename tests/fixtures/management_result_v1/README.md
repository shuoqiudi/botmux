# Management Result v1 samples

Copied unchanged from `shuoqiudi/it_manage`, issue #1253 (merged PR #1254),
`tests/fixtures/management_result_v1/`. These protocol samples cover success,
failure, empty, partial timeout, unavailable and legacy output. They are test
inputs, not evidence of live Telegram delivery.
